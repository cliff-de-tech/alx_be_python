#!/usr/bin/python3

# Define the number of hours to convert
hours = 2

# Calculate seconds (3600 seconds in an hour)
seconds = hours * 3600

# Print the result
print(f"{hours} hour(s) is {seconds} seconds.")